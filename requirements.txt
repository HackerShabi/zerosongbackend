# FastAPI and server dependencies
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6

# Audio processing
spleeter==2.1.0
pydantic<2
tensorflow==2.8.4
librosa==0.9.2
soundfile==0.10.3
numpy==1.21.6
scipy==1.7.3
pandas==1.3.5
norbert==0.2.1
musdb==0.4.0
museval==0.4.0
stempeg==0.1.8
ffmpeg-python==0.2.0

# Additional utilities
pydub==0.25.1
requests==2.31.0

# For better performance on CPU
tensorflow-cpu==2.13.0

# System dependencies (may need to be installed separately)
# ffmpeg - required for audio processing
# Note: On Render, ffmpeg should be available by default
# For local development on Windows, install ffmpeg separately